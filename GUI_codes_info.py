        # # Create a toggle button for locking/unlocking door
        # lock_state = tk.BooleanVar()
        # lock_state.set(False)

# def toggle_lock():
#     if lock_state.get():
#         lock_state.set(False)
#         lock_unlock_button.config(text="Lock Door")
#     else:
#         lock_state

    # def popupmsg(msg):
    #     popup = Toplevel()
    #     popup.geometry('200x100')
    #     popup.title('Message')
    #     Label(popup, text=msg, font=('Verdana', 12)).pack(pady=10)
    #     Button(popup, text='OK', command=popup.destroy).pack()